<%- include('partials/header-logged-in') -%>
<div class="container card-container">
  <!-- <div class="row justify-content-center mt-5">
    <div class="col-6">
      <h2><%= post.title %></h2>
      <p><%= post.caption %></p>
      <p>By: <%= user.User %></p>
      <img class="img-fluid" src="<%= post.image%>" />
      <div class="row justify-content-between">
        <form
          class="col-1"
          action="/post/likePost/<%= post.id %>?_method=PUT"
          method="POST"
        >
          <button class=" <%if(post.likes.includes(user.id)){ %> btn-liked <%} %>
          btn btn-primary fa fa-heart" type="submit"></button>
        </form>
        <h3 class="col-3">Likes: <%= post.likes.length - 1 %></h3>
        <form
          class="col-1"
          action="/post/bookmarkPost/<%= post.id %>?_method=PUT"
          method="POST"
        >
        <button class=" <%if(post.bookmarks.includes(user.id)){ %> bi bi-bookmark-fill <%} else { %> bi bi-bookmark <% }%>
          btn btn-primary" type="submit"></button>
        </form>
        <%if(post.user == user.id){ %>
        <form
          action="/post/deletePost/<%= post.id %>?_method=DELETE"
          method="POST"
          class="col-3"
        >
          <button class="btn btn-primary fa fa-trash" type="submit"></button>
        </form>
        <%}%>
      </div>
    </div>
    <div class="col-3 mt-5">
      <h3>Description</h3>
      <% for(var i=0; i<post.chDescription.length; i++) {%>
      <p>- <%= post.chDescription[i] %></p>
      <% } %>
      <h3>Additional Colors</h3>
      <ol class="list-group list-group-numbered">
        <% for(var i=0; i<post.addColors.length; i++) {%>
          <li class=""><%= post.addColors[i] %></li>
        <% } %>
        </ol>
        <p><%= post.addColors %></p> -->
    <!-- </div>
    <div class="col-6 mt-5">
      <a class="btn btn-primary" href="/profile">Return to Profile</a>
      <a class="btn btn-primary" href="/feed">Return to Feed</a>
    </div>
  </div> --> 

    <!-- <h1><%= post.title %></h1>
    <div class="row mt-5">
      <div class="col-6">
        <img src="<%= post.image%>" class="img-fluid" alt="responsive image">
      </div>
    </div>
    <div class="row">
      <div class="col-1">
        <h3>Character</h3>
        <h2>Description</h2>
        <ul class="list-group">
        <% for(var i=0; i<post.chDescription.length; i++) {%>
            <li class=""><%= post.chDescription[i] %></li>
            <% } %>
          </ul>
        <h2>Additional Colors</h2>
        <ol class="list-group list-group-numbered">
          <% for(var i=0; i<post.addColors.length; i++) {%>
          <li class=""><%= post.addColors[i] %></li>
          <% } %>
        </ol>
      </div>
    </div> -->
 
    <div class="row">
      <h1 class="display-4"><%= post.title %></h1>
      <p><%= post.caption %></p>
      <p class="accent-1">By: <%= post.createdBy %></p>
      <div class="col-md-6">
        <img src="<%= post.image%>" class="img-fluid" alt="Responsive image">
        <div class="row row-cols-3">
          <form
            class="col-2"
            action="/post/likePost/<%= post.id %>?_method=PUT"
            method="POST"
          >
            <button class=" <%if(post.likes.includes(user.id)){ %> btn-liked <%} %>
            btn btn-primary fa fa-heart" type="submit"></button>
          </form>
          <span class="col"><%= post.likes.length - 1 %></span>
          <form
            class="col d-flex justify-content-end"
            action="/post/bookmarkPost/<%= post.id %>?_method=PUT"
            method="POST"
          >
          <button class="<%if(post.bookmarks.includes(user.id)){ %> bi bi-bookmark-fill<%} else { %> bi bi-bookmark <% }%>
            btn btn-primary" type="submit"></button>
          </form>
          <%if(post.user == user.id){ %>
          <form
            action="/post/deletePost/<%= post.id %>?_method=DELETE"
            method="POST"
            class="col-3"
          >
            <button class="btn btn-primary fa fa-trash" type="submit"></button>
          </form>
          <%}%>
        </div>
      </div>
      <div class="col-md-6">
        <!-- <h3 class="display-4">Character</h3> -->
        <h2>Description</h2> <hr class="style1">
        <ul class="list-unstyled">
        <% for(var i=0; i<post.chDescription.length; i++) {%>
            <li class="p-1 g-2"><%= post.chDescription[i] %></li>
            <% } %>
        </ul>
        <h2>Additional Colors</h2> <hr class="style1">
        <ul class="list-unstyled">
          <% for(var i=0; i<post.addColors.length; i++) {%>
          <li class="p-1 g-2"><%= post.addColors[i] %></li>
          <% } %>
        </ul>
        <h2>Price Range</h2> <hr class="style1">
        <p class="p-1"><%= post.priceRange %></p>
        <!-- <ul class="list-unstyled"> -->
        <!-- <% for(var i=0; i<post.priceRange.length; i++) {%> -->
            <!-- <li class="p-1"><%= post.priceRange %></li> -->
            <!-- <% } %> -->
        <!-- </ul> -->
        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
          <a href="" type="button" class="btn btn-primary btn-lg">
            Book Now
          </a>
          <!-- <button type="button" class="btn btn-outline-secondary btn-lg px-4">Default</button> -->
        </div>
      </div>
    </div>
</div>

<!-- <div class="container">
  <div class="card" style="width: 50rem;">
    <h3 class="card-title"><%= post.title %></h3>
    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
    <img src="<%= post.image%>" class="card-img-top" alt="...">
    <div class="card-body">
    </div>
    <ul class="list-group list-group-flush">
      <li class="list-group-item">An item</li>
      <li class="list-group-item">A second item</li>
      <li class="list-group-item">A third item</li>
    </ul>
    <div class="card-body">
      <a href="#" class="card-link">Card link</a>
      <a href="#" class="card-link">Another link</a>
    </div>
  </div>
  </div> -->

<%- include('partials/footer') -%>
